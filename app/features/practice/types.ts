export type PracticeCard = {
  word: string;
  translation: string;
  example: string;
  emoji: string;
};

export type UsefulPhrase = {
  phrase: string;
  translation: string;
  emoji: string;
};

export type ListeningChallenge = {
  phrase: string;
  options: string[];
  translation: string;
};
